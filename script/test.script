import "std/io.script"
import "std/types.script"

void main()
{
    string input = GetLine();
    int test = ToInt(input);
    if (test == 3)
    {
        WriteLine("scope");
    }
    WriteLine(ToString(test));
}