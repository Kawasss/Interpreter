import "std/io.script"
import "std/types.script"

void PrintFibonacciSequence(int amount)
{
    int x;
    int y = 1;
    int z;
    
    for (int i = 0; i < amount; i++)
    {
        WriteLine(ToString(x));
        string padding1;
        int test = 0;

        z = x + y;
        x = y;
        y = z;
    }
    return;
}

void main()
{
    WriteLine("test");

    int padding;

    int test = 43242;
    int test1 = &test;
    int test2 = *test1;

    WriteLine(IntToString(test2)); # the derefenced value
    WriteLine(IntToString(test1)); # the pointer value

    PrintFibonacciSequence(ToInt(GetLine()));
}